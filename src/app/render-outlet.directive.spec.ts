import { RenderOutletDirective } from './render-outlet.directive';

describe('RenderOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderOutletDirective();
    expect(directive).toBeTruthy();
  });
});
